{
  "name": "@sigillum/daemon-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "rm -rf .tmp/ui-tests && tsc -p tsconfig.test.json && printf '{\"type\":\"commonjs\"}' > .tmp/ui-tests/package.json && node --test .tmp/ui-tests/test/*.test.js",
    "build": "vite build"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.0"
  }
}
